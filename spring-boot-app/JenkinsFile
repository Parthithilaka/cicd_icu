pipeline {
  agent {
     docker {
      image "parthi430/ultimate-cicd-pipeline:v1"
      args "--user root -v /var/run/docker.sock:/var/run/docker.sock"
    }
  }
  stages {
    stage('SCM checkout'){
      steps {
          sh "echo passed"
      }
    }
  }
}